import React from "react";
import { ScrollView, Text } from "react-native";
import { styled } from "nativewind";

export const Page = styled(ScrollView, "p-3 bg-background");

export const Txt = styled(Text, "text-default");
